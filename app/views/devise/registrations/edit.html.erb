<h2>Настройки учетной записи </h2>
<hr>
<div class="row">
  <div class="col-sm-7">
    <h3>Изменение учетной записи</h3>
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="jumbotron">
        <h4>Песональные изменения</h4>
        <div class="row">
          <div class="form-group col-md-12">
            <%= f.label :name, 'Имя' %><br>
            <%= f.text_field :name, autofocus: true, autocomplete: "email", class: "form-control" %>
          </div>
        </div>
  
        <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
          <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
        <% end %>
  
        <div class="row">
          <div class="form-group col-md-12">
            <%= f.label :password, 'Дата рождения' %> <i><small></small></i><br>
            <%= f.date_field :date_of_birth, as: :date, value: f.object.try(:strftime,"%m/%d/%Y"), autocomplete: "new-password", class: "form-control", placeholder: "минимум 6 символов" %>
          </div>
        </div>
  
        <div class="row">
          <div class="form-group col-md-12">
            <%= f.label :sex, 'Пол'  %><br>
            <%= f.radio_button(:sex, "male") %>
            <%= label :sex_male, 'мужчина' %>
            <%= f.radio_button(:sex, "female") %>
            <%= label :sex_female, 'женщина' %>
          </div>
        </div>

        <div class="row">
          <div class="form-group col-md-3">
            <%= f.label :weigth, 'Вес (кг)'  %><br>
            <%= f.number_field  :weigth, min: 10, class: "form-control" %>
          </div>
          <div class="form-group col-md-3">
            <%= f.label :growth, 'Рост (см)'  %><br>
            <%= f.number_field  :growth,  min: 50, class: "form-control" %>
          </div>
          <div class="form-group col-md-3">
            <%= f.label :activity, 'Активность'  %><br>
            <%= f.select(:activity, [['Минимальная', '1.2'],['Низкая', '1.375'],['Средняя', '1.55'],['Высокая', '1.725'],['Очень высокая', '1.9']]) %>
          </div>
        </div>
      </div>

      <div class="jumbotron">
        <h4>Глобальные изменения</h4>
        <div class="row">
          <div class="form-group col-md-12">
            <%= f.label :email, 'Почта' %><br>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
          </div>
        </div>
  
        <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
          <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
        <% end %>
  
        <div class="row">
          <div class="form-group col-md-12">
            <%= f.label :password, 'Пароль' %> <i><small>(оставьте это поле пустым, если не хотите изменять)</small></i><br>
            <%= f.password_field :password, autocomplete: "new-password", class: "form-control", placeholder: "минимум 6 символов" %>
          </div>
        </div>
  
        <div class="row">
          <div class="form-group col-md-12">
            <%= f.label :password_confirmation, 'Подтверждение нового пароля'  %><br>
            <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control" %>
          </div>
        </div>
      </div>

      <div class="row">
        <div class="form-group col-md-12">
          <%= f.label :current_password, 'Подтверждение изменений' %> <i><small>(введите текущий пароль для подтверждения изменений)</small></i><br>
          <%= f.password_field :current_password, autocomplete: "current-password", class: "form-control" %>
        </div>
      </div>

      <div class="actions">
        <%= f.submit "Сохранить", class: "btn btn-success btn-lg-4" %>
      </div>
    <% end %>
  </div>

  <div class="col-sm-5">
    <h3>Удаление учетной записи</h3>
    <p>Желаете покинуть нас? <%= button_to "Удалить мой аккаунт", registration_path(resource_name), data: { confirm: "Вы уверены?" }, method: :delete, class: "btn btn-danger" %></p>
  </div>
</div>
<hr>
<%= link_to "Назад", :back, class: "btn btn-secondary btn-lg btn-block" %>
