<li>
  <div class="ingredient">
    <%= composition.ingredient.title %>
  </div>
  <div class="ingredient-value">
    <%= composition.weight %>г  
    <% if can? :update, composition.recipe %>
      <%= link_to image_tag('pencil.png'), edit_composition_path(composition.id) %> 
    <% end %>
  </div>
</li>

